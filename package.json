{
  "name": "verycool-dev-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Dev server + proxy for VeryCool (local dev to avoid CORS issues)",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "node-fetch": "^2.6.9"
  }
}